{
  "name": "brevo-proxy",
  "version": "1.0.0",
  "type": "module",
  "description": "Thin HTTP proxy that forwards requests to Brevo's transactional email API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/server.test.js"
  },
  "dependencies": {
    "dotenv": "17.4.2",
    "express": "5.2.1",
    "node-fetch": "3.3.2"
  },
  "engines": {
    "node": ">=24"
  }
}
